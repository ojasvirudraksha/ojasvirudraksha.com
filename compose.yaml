name: ojasvirudraksha
services:
  web:
    build: .
    ports:
      - "127.0.0.1:${OJASVIRUDRAKSHA_PORT:-8000}:8000"
    env_file: .env
    depends_on:
      mysql:
        condition: service_healthy
    volumes:
      - local_state:/app/.local
      - uploads:/app/media

  mysql:
    image: mysql:8.4
    env_file: .env
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "MYSQL_PWD=$$MYSQL_PASSWORD mysql --protocol=TCP -h 127.0.0.1 -u $$MYSQL_USER $$MYSQL_DATABASE -e 'SELECT 1' >/dev/null 2>&1"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 30s

volumes:
  mysql_data:
  local_state:
  uploads:
